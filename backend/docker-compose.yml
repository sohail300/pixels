name: pixels

services:
  backend:
    build: .
    container_name: pixels-backend
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8000}
    restart: unless-stopped
